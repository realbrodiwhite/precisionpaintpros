import { renderHook } from '@testing-library/react';
import { useTestimonials } from '../useTestimonials';

describe('useTestimonials', () => {
  it('should return the testimonials', async () => {
   
    
    
  });
});